<%= form_for(@micropost) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :content, placeholder: "今日の一言" %>
  </div>
  <%= f.submit "twitter", class: "btn btn-primary" %>
<% end %>