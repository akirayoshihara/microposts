<%= form_for(current_user.bookmarks_relationships.find_by(micropost_id: micropost.id),
             html: { method: :delete },
             remote: true) do |f| %>
  <%= f.submit "★", class: "btn" %>
<% end %>